packages:
  - "packages/*"
  - "apps/*"

# pnpm 11: explicitly allow postinstall for packages that need native binaries
allowBuilds:
  "@prisma/client": true
  "@prisma/engines": true
  prisma: true
  esbuild: true
  unrs-resolver: true
